---
const assetId = new URLSearchParams(Astro.request.url).get('assetid') ?? '';
---

<html lang="nl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>QR Melding – Atalian</title>
    <style>
      body {
        font-family: sans-serif;
        padding: 2rem;
        max-width: 600px;
        margin: auto;
      }
      label {
        display: block;
        margin-top: 1rem;
      }
      input, textarea {
        width: 100%;
        padding: 0.5rem;
        margin-top: 0.25rem;
        font-size: 1rem;
      }
      button {
        margin-top: 1.5rem;
        padding: 0.75rem 1.5rem;
        background-color: #f07c10;
        color: white;
        border: none;
        font-size: 1rem;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <h1>Probleem melden</h1>
    <p>Gebruik dit formulier om een storing of defect door te geven via QR-code.</p>

    <form method="post" onsubmit="handleSubmit(event)">
      <label for="email">Uw e-mailadres</label>
      <input id="email" name="email" type="email" required />

      <label for="beschrijving">Probleembeschrijving</label>
      <textarea id="beschrijving" name="beschrijving" required rows="5"></textarea>

      <label for="assetid">Asset-ID</label>
      <input id="assetid" name="assetid" type="text" value={assetId} readonly />

      <button type="submit">Verzend melding</button>
    </form>

    <script type="text/javascript">
      async function handleSubmit(e) {
        e.preventDefault();
        const form = e.target;

        // Bouw platte JSON-body, exact als in je VBA-script
        const body = {
          JobDescr: form.beschrijving.value,
          ReportText: form.email.value,
          EquipmentId: "",
          ExternalId: "",
          PriorityId: "",
          ServiceDeskReportTypeId: "601",
          SpaceId: form.assetid.value
        };

        try {
          const res = await fetch("https://atalian-test.ultimo.net/api/v1/action/_REST_OneAtalianJob", {
            method: "POST",
            headers: {
              "accept": "application/json",
              "ApiKey": "235A210503214055A27F070DB8748750",
              "ApplicationElementId": "3f92bbfca30445ff875f3a9d956441be",
              "Content-Type": "application/json"
            },
            body: JSON.stringify(body)
          });

          if (res.ok) {
            alert("✅ Melding succesvol doorgestuurd naar Ultimo!");
            form.reset();
          } else {
            const errorText = await res.text();
            console.error("Ultimo API-fout:", errorText);
            alert("❌ Fout bij verzending naar Ultimo:\n" + errorText);
          }
        } catch (err) {
          console.error("Netwerkfout:", err);
          alert("⚠️ Verbindingsprobleem bij Ultimo.");
        }
      }
    </script>
  </body>
</html>
