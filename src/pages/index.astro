---
const assetId = new URLSearchParams(Astro.request.url).get('assetid') ?? '';
---

<html lang="nl">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>QR Melding – Atalian</title>
    <style>
      body {
        font-family: sans-serif;
        padding: 2rem;
        max-width: 600px;
        margin: auto;
      }
      label {
        display: block;
        margin-top: 1rem;
      }
      input, textarea {
        width: 100%;
        padding: 0.5rem;
        margin-top: 0.25rem;
        font-size: 1rem;
      }
      button {
        margin-top: 1.5rem;
        padding: 0.75rem 1.5rem;
        background-color: #f07c10;
        color: white;
        border: none;
        font-size: 1rem;
        cursor: pointer;
      }
    </style>
  </head>
  <body>
    <h1>Probleem melden</h1>
    <p>Gebruik dit formulier om een storing of defect door te geven via QR-codes.</p>

    <form method="post" onsubmit="handleSubmit(event)">
      <label for="email">Uw e-mailadres</label>
      <input id="email" name="email" type="email" required />

      <label for="beschrijving">Probleembeschrijving</label>
      <textarea id="beschrijving" name="beschrijving" required rows="5"></textarea>

      <label for="assetid">Asset-ID</label>
      <input id="assetid" name="assetid" type="text" value={assetId} readonly />

      <button type="submit">Verzend melding</button>
    </form>

  
  </body>
</html>
